var app = require("../models/app.js");

module.exports = function(app) {
    //route to put line in wishlist table and display to website

    //route to move line from wishlist to "viewed" table and display to website

    //route to move line from wishlist to "deleted" table and display to website










}